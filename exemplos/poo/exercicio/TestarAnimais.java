public class TestarAnimais {
    
    public static void main(String[] args) {
        // Animal animal = new Animal();

        // animal.obterDados();

        // Animal animal2 = new Animal("Camelo", 1.5f, 4, null, null, 12.5f);

        // animal.setComprimento(1.5f);

        // animal.obterDados();

        // Peixe peixe = new Peixe("Tubarão", 300, 0, "Cinzento", "Mar", 1.5f, "Barbatanas e Cauda");

        // peixe.obterDados();

        Mamifero mamifero = new Mamifero("Baleia", 0, 0, null, null, 0, null);

        mamifero.obterDados();
    }

}
